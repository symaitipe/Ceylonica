server:
  port: 8086

spring:
  application:
    name: payment-service

  mongodb:
    uri: mongodb://localhost:27017/ceylonica_payments

stripe:
  secret-key:
  publishable-key:

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true